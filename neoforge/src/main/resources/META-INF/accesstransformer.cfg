# Widens the private ItemProperties.register overload (Yarn: ModelPredicateProviderRegistry.register with a
# ClampedModelPredicateProvider) that WitcherModelPredicates calls to register the aerondight/iris charge predicates.
public net.minecraft.client.renderer.item.ItemProperties register(Lnet/minecraft/world/item/Item;Lnet/minecraft/resources/ResourceLocation;Lnet/minecraft/client/renderer/item/ClampedItemPropertyFunction;)V

# Widens the private final LootPool.entries field that Spell Engine's LootHelper.inspect reads directly
# (Mojang-mapped name confirmed via javap on the merged NeoForge minecraft jar; runtime package for this
# project's layered mappings is net.minecraft.loot, not the vanilla-official net.minecraft.world.level.storage.loot).
public net.minecraft.loot.LootPool entries
